---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-- TKPROF.sql
-- 오라클 관리자 도구인 TKPROF를 사용하기 위한 설정
-- 관리자 권한이 필요
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-- 감시를 시작하도록 설정
ALTER SESSION SET SQL_TRACE = TRUE;

-- 감시 결과를 저장할 파일의 구분자 설정
ALTER SESSION SET TRACEFILE_IDENTIFIER = 'WHAT';

-- 감시 설정이 끝났으니 SQL 명령을 실행
select*from dba_users;

select*from dba_roles;

--실행이 완료되면 .trc 파일이 생김(오라클 설치폴더에)
--찾아서 TKPROF 도구로 변환하면 끝
--변환은 commandline 에서 진행